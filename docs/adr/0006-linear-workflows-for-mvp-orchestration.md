# Use Linear Workflows for MVP Orchestration

Novel Agent MVP Orchestration Configurations run Agent Assignments in a user-configured linear order. Arbitrary graphs or shared blackboard coordination are deferred because the first version needs debuggable, traceable Generation Workflows where each Agent Output is passed downstream predictably and failures are easy to inspect in Workflow Traces.
