# Use Next.js App Router for the MVP Web UI

Novel Agent uses Next.js App Router as the MVP Web UI and local backend framework. The product needs forms, route-level workspaces, server-side SQLite access, and fast iteration across UI and application actions; using one full-stack TypeScript framework avoids prematurely splitting a React frontend from a separate API service.
